{
  "name": "@homelab/server-kit",
  "version": "1.0.0",
  "description": "Shared Express bootstrap for the homelab apps: createApp, health/error handlers, and startServer with graceful shutdown",
  "private": true,
  "type": "module",
  "main": "app.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "rotating-file-stream": "^3.2.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "express": "^5.1.0",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  }
}
