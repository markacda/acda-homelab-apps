{
  "name": "@homelab/db",
  "version": "1.0.0",
  "description": "Shared PostgreSQL kit for the homelab apps: a pool factory, a SQL-file migration runner, and a health-check ping",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=24"
  }
}
