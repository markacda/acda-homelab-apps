{
  // Production build: emit only the runtime code (just server.ts — this app has
  // no server-side domain). Tests are still type-checked via tsconfig.json
  // (npm run typecheck) but are never emitted, keeping dist/ free of test files.
  "extends": "./tsconfig.json",
  "include": ["server.ts"]
}
