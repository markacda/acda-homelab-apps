node_modules/
npm-debug.log*
.env
.DS_Store

# TypeScript build output
dist/

# Compiled client bundles emitted from client/*.ts into the served public/ dir
apps/*/public/*.js
apps/*/public/*.js.map
# Same, for apps on the DDD layout that serve from Web/public/
apps/*/Web/public/*.js
apps/*/Web/public/*.js.map
# ...but the PWA service worker is hand-written source, not a compiled bundle.
!apps/dashboard/Web/public/sw.js

# Local price-cache written by dynamic-vs-fixed in dev (DATA_DIR)
data/

# Local access logs written by each app in dev (LOG_DIR)
logs/
