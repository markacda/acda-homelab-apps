node_modules
package-lock.json
data
dist

# Compiled client output (generated from client/*.ts)
apps/*/public/*.js

# atc ships vendored frontend libs / browser JS with no build pipeline
apps/atc/Web/public

# atc cached upstream JSON snapshots served on proxy failure (data, not source)
apps/atc/proxy-fallback
